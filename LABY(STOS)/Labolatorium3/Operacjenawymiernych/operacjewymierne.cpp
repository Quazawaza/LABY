#include <iostream>

using namespace std;

int main()
{
    char command;
    while (cin >> command)
    {
        int a;
        int b;
        if (command == 'A')
        {
            cin >> a;
        }
        cout << a;
    }
    
    
}